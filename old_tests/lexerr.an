/*
        lexerr.an
    shows possible types of errors caught during the lexing
    phase.  Since lexing errors are almost always fatal, compilation
    is usually halted after the first incidence.
*/

//Extraneous numbers after type suffix
let i = 5_i321

//Types must not contain an _ character
let v = My_Str "hi"

//And worst of all, use of the tab character in significant whitespace
if ans == i then
	puts "ans == i"
