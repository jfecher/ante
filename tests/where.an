`
        where.an
`


~Constants declared with where after an expression are equivalent 
~to let bindings declared beforehand.
let str = hello \
    where hello = "Hello World!"

~Where clauses are right associative and have very low priority
let five = three + two where three = 3 where two = 2

printf("%s\nthree = %d\n", str, five - two where two = 2)


~Returns the sum of an infinite series such as 1 + 1/2 + 1/4 + ...
~Parameters:
~   a: the first value of the series
~   r: the common ratio
~Prereqs: Assumes r is a valid ration between 0 and 1 exclusive.
f64 sumOfSeries: f64 a r
    return a / (1.0 - r)


printf("The sum of the series 1 + 1/2 + 1/4 + 1/8 + ... + 1/2^(n-1) is %d\n", sumOfSeries(1.0, 1.0/2.0))
