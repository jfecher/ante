`
    stdlib.zy
        tests functions in stdlib for expected output when given
        a variety of types and number of variables.
`

i32 i = 1234
str ls = 'ls '
str opt = ' --color=auto '

system(ls .. opt .. "..")
typeof(ls)

system(ls, opt)
typeof(ls, opt)

typeof(5, 3.5, 'test')
typeof()
