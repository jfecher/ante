// Regression: an `if .. is .. then` with no else should return unit.
// These desugar into match expressions, hence the original error
foo (x: U32): Unit =
    if x is 0 then
        42

main () = ()

// args: --check
