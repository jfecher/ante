foo x =
    y = fn _ -> x
    y

// type should be a -> (b -> a)

// args: --show-types
// expected stdout:
// (foo: fn _ -> fn _ -> _) (x: _) =
//     (y: fn _ -> _) (_: _) = (x: _)
//     (y: fn _ -> _)

