foo x =
    y = fn _ -> x
    y

// type should be a -> (b -> a)

// args: -e ast-t
// expected stdout:
// (foo: fn _ -> fn _ -> _ can _ can _) (x: _) =
//     (y: fn _ -> _ can _) (_: _) = (x: _)
//     (y: fn _ -> _ can _)

