
foo () = ()

bar (a, b, c) = (a, b, c)

baz (a, b) c = ()

// args: --parse
// expected stdout:
// (foo = (\ () . ()));
// (bar = (\ (',' a (',' b c)) . (',' a (',' b c))));
// (baz = (\ (',' a b) c . ()))
