import Std.Env
import Std.Vec
import Std.Stream.stream_fn

main () =
    abort_on_panic do
    v = Vec.of Env.args
    println (v.len () >= 1)

// args: --delete-binary --no-color
// expected stdout: true

