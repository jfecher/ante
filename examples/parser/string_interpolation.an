main () =
    shout x = "${x}!"
    bar = "nested interpolations"
    println
        "I can do line breaks${
            shout "\nAny expressions"
        }${
            shout "\nEven ${bar}"
        }"

// args: -e ast --no-color
// expected stdout:
// main () =
//     shout x = "${x}!"
//     bar = "nested interpolations"
//     println ("I can do line breaks${shout "
// Any expressions"}${shout ("
// Even ${bar}")}")

