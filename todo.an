// refactor checkTyInTraitDecl + fn version of functions
// big code cleanup
// error if creating a new type with same name as type family, but not implementing type family
// parsing error:  Str (i32 x)
//
// impl In (Vec 't) 't  should type error  (should be In 't (Vec 't))
// copyWithNewTypeVars(traittype and trait decls) before constraintfindingisitor::visit(ExtNode)

// Stop duplicating print(Str) print.1(Str) print.2(Str) ...
// Fix #  not overloadable
// Fix != not overloadable
